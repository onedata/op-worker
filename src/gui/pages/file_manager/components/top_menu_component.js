FileManager.TopMenuComponent = Ember.Component.extend({
    needs: 'application'
    //global: {
    //    user: 'user lol'
    //}
});

FileManager.GlobalController = Ember.Controller.extend({
    //global: {
    //    user: 'user lol global'
    //}
});