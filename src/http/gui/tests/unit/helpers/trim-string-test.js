import { trimString } from '../../../helpers/trim-string';
import { module, test } from 'qunit';

module('Unit | Helper | trim string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trimString(42);
  assert.ok(result);
});
