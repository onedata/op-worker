import fileChunksBar from '../../../utils/file-chunks-bar';
import { module, test } from 'qunit';

module('Unit | Utility | file chunks bar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileChunksBar();
  assert.ok(result);
});
