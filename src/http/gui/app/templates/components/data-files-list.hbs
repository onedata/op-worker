Data files list for

<ul>
  {{#each filesInfo as |fileInfo|}}
    <li {{action "openFile" fileInfo.file}}>
      <span>
        {{#if fileInfo.file.isDir}}
          {{icon 'folder'}}
        {{else}}
          {{icon 'file'}}
        {{/if}}
      </span>
      <strong class="file-entry" id="file-{{fileInfo.file.id}}" {{action 'selectFile' fileInfo.file}}>
        {{fileInfo.file.name}}
      </strong>
    </li>
  {{/each}}
</ul>

<div class="create-file">
  {{input value=createFileName}}
  <a {{action 'createFile' 'file'}} style="margin-right: 15px;">CREATE-FILE</a>
  <a {{action 'createFile' 'dir'}}>CREATE-DIRECTORY</a>
</div>
