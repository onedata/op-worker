{{!-- TODO: use bs-select (which does not show labels for strange reason...) --}}
{{!-- TODO: or style select with bootstrap --}}
{{view 'select' content=spaces optionValuePath="content.id"
       optionLabelPath="content.name" value=selectedSpaceId}}
