package one.clproto;

import "common_messages.proto";

message HandshakeRequest {
    repeated EnvironmentVariable     environment_variables    = 2;
}

message HandshakeResponse {
    required bytes fuse_id = 1;
}

message HandshakeAcknowledgement {
    required bytes fuse_id = 1;
}