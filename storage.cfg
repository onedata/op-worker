% format: [ DirectIOStorage1, DirectIOStorage2 ...].
% where
%    DirectIOStorage :: [{name,Name},{root,Root}]
%    Name :: atom()
%    Root :: string()
% DirectIOStorage1 always corresponds to main storage used by cluster (to store his files) and must have name set to: cluster_fuse_id,
% others are optional and define additional directIO storages used by specific groups of fuse clients.
% Each line represents one storage.

[[{name,cluster_fuse_id},{root,"/mnt/vfs"}]].
