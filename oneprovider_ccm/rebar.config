%% directory for releases
{sub_dirs, ["rel"]}.

%% to include deps headers
{lib_dirs, ["deps"]}.

%% deps options
{deps, [
    {ctool,".*", {git, "ssh://git@git.plgrid.pl:7999/vfs/ctool.git", {tag, "572173870a1"}}},
    {node_package, ".*", {git, "git://github.com/xorver/node_package.git", {tag, "83b1f02cabb"}}}
]}.

{pre_hooks, [
    % gen_dev compilation
    {clean, "make -C bamboos/gen_dev/ clean"},
    {compile, "make -C bamboos/gen_dev/ compile"}
]}.
