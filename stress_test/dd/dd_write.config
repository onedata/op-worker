%% Test used to check preformance of ClusterProxy write operation 
{mode, max}.
{duration, 60}.
{concurrent, 3}.

{driver, veilcluster_driver_dd}.

{operations, [{write, 1}]}.
{block_size, 128}. %% In KBytes
{max_filesize, 10}. %% In MBytes